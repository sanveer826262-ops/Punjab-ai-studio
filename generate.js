export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});const key=process.env.FAL_KEY;if(!key)return res.status(500).json({error:"FAL_KEY is not configured in Vercel"});try{const {mode="text",prompt,image,aspect_ratio="9:16",resolution="480p"}=req.body||{};if(!prompt)return res.status(400).json({error:"Prompt required"});const model=mode==="image"?"fal-ai/wan-i2v":"fal-ai/wan-t2v";const input={prompt,aspect_ratio,resolution,num_frames:81,frames_per_second:16,enable_safety_checker:true,enable_prompt_expansion:true};if(mode==="image"){if(!image)return res.status(400).json({error:"Image required"});input.image_url=image}const r=await fetch("https://queue.fal.run/"+model,{method:"POST",headers:{"Authorization":"Key "+key,"Content-Type":"application/json"},body:JSON.stringify(input)});const d=await r.json();if(!r.ok)return res.status(r.status).json({error:d?.detail||d?.message||"fal.ai request failed"});res.status(200).json({request_id:d.request_id,model})}catch(e){res.status(500).json({error:e.message||"Server error"})}}